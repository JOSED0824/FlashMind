abstract final class AppDurations {
  static const sessionDuration = Duration(minutes: 7);
  static const animFast = Duration(milliseconds: 200);
  static const animNormal = Duration(milliseconds: 400);
  static const animSlow = Duration(milliseconds: 700);
  static const splashDelay = Duration(seconds: 2);
}
